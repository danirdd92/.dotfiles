abbr -g "..."="../.."
abbr -g "...."="../../.."
abbr -g "....."="../../../.."
abbr -g "......"="../../../../.."
abbr "-"="cd -"
abbr "1"="cd -1"
abbr "2"="cd -2"
abbr "3"="cd -3"
abbr "4"="cd -4"
abbr "5"="cd -5"
abbr "6"="cd -6"
abbr "7"="cd -7"
abbr "8"="cd -8"
abbr "9"="cd -9"
abbr "_"="sudo "
abbr "c"="clear"
abbr "cat"="batcat -p"
abbr "catt"="batcat"
abbr "compose"="docker compose"
abbr "dcd"="docker compose down"
abbr "dcp"="docker cp"
abbr "dcu"="docker compose up"
abbr "dex"="docker exec -it"
abbr "dlo"="docker logs -f"
abbr "drmca"="docker rm -f "
abbr "drmia"="docker rmi -f "
abbr "egrep"="grep -E --color=auto --exclude-dir={.bzr,CVS,.git,.hg,.svn,.idea,.tox}"
abbr "fgrep"="grep -F --color=auto --exclude-dir={.bzr,CVS,.git,.hg,.svn,.idea,.tox}"
abbr "ga"="git add"
abbr "gc"="git commit -am"
abbr "gco"="git checkout"
abbr "glo"="git log --oneline --graph"
abbr "gloa"="git log --oneline --graph --all"
abbr "globurl"="noglob urlglobber "
abbr "grep"="grep --color=auto --exclude-dir={.bzr,CVS,.git,.hg,.svn,.idea,.tox}"
abbr "gs"="git status"
abbr "history"="omz_history"
abbr "k"="kubectl"
abbr "l"="eza -l --icons"
abbr "la"="eza -a --icons"
abbr "ll"="eza -la --icons"
abbr "llt"="eza -laT --level=2 --icons"
abbr "lltt"="eza -laT --level=3 --icons"
abbr "llttt"="eza -laT --level=4 --icons"
abbr "ls"="eza"
abbr "lsa"="ls -lah"
abbr "md"="mkdir -p"
abbr "rd"="rmdir"
abbr "sc-add-requires"="sudo systemctl add-requires"
abbr "sc-add-wants"="sudo systemctl add-wants"
abbr "sc-cancel"="sudo systemctl cancel"
abbr "sc-cat"="systemctl cat"
abbr "sc-daemon-reexec"="sudo systemctl daemon-reexec"
abbr "sc-daemon-reload"="sudo systemctl daemon-reload"
abbr "sc-default"="sudo systemctl default"
abbr "sc-disable"="sudo systemctl disable"
abbr "sc-disable-now"="sc-disable --now"
abbr "sc-edit"="sudo systemctl edit"
abbr "sc-emergency"="sudo systemctl emergency"
abbr "sc-enable"="sudo systemctl enable"
abbr "sc-enable-now"="sc-enable --now"
abbr "sc-failed"="systemctl --failed"
abbr "sc-get-default"="systemctl get-default"
abbr "sc-halt"="sudo systemctl halt"
abbr "sc-help"="systemctl help"
abbr "sc-hibernate"="systemctl hibernate"
abbr "sc-hybrid-sleep"="systemctl hybrid-sleep"
abbr "sc-import-environment"="sudo systemctl import-environment"
abbr "sc-is-active"="systemctl is-active"
abbr "sc-is-enabled"="systemctl is-enabled"
abbr "sc-is-failed"="systemctl is-failed"
abbr "sc-is-system-running"="systemctl is-system-running"
abbr "sc-isolate"="sudo systemctl isolate"
abbr "sc-kexec"="sudo systemctl kexec"
abbr "sc-kill"="sudo systemctl kill"
abbr "sc-link"="sudo systemctl link"
abbr "sc-list-dependencies"="systemctl list-dependencies"
abbr "sc-list-jobs"="systemctl list-jobs"
abbr "sc-list-machines"="sudo systemctl list-machines"
abbr "sc-list-sockets"="systemctl list-sockets"
abbr "sc-list-timers"="systemctl list-timers"
abbr "sc-list-unit-files"="systemctl list-unit-files"
abbr "sc-list-units"="systemctl list-units"
abbr "sc-load"="sudo systemctl load"
abbr "sc-mask"="sudo systemctl mask"
abbr "sc-mask-now"="sc-mask --now"
abbr "sc-poweroff"="systemctl poweroff"
abbr "sc-preset"="sudo systemctl preset"
abbr "sc-preset-all"="sudo systemctl preset-all"
abbr "sc-reboot"="systemctl reboot"
abbr "sc-reenable"="sudo systemctl reenable"
abbr "sc-reload"="sudo systemctl reload"
abbr "sc-reload-or-restart"="sudo systemctl reload-or-restart"
abbr "sc-rescue"="sudo systemctl rescue"
abbr "sc-reset-failed"="sudo systemctl reset-failed"
abbr "sc-restart"="sudo systemctl restart"
abbr "sc-revert"="sudo systemctl revert"
abbr "sc-set-default"="sudo systemctl set-default"
abbr "sc-set-environment"="sudo systemctl set-environment"
abbr "sc-set-property"="sudo systemctl set-property"
abbr "sc-show"="systemctl show"
abbr "sc-show-environment"="systemctl show-environment"
abbr "sc-start"="sudo systemctl start"
abbr "sc-status"="systemctl status"
abbr "sc-stop"="sudo systemctl stop"
abbr "sc-suspend"="systemctl suspend"
abbr "sc-switch-root"="sudo systemctl switch-root"
abbr "sc-try-reload-or-restart"="sudo systemctl try-reload-or-restart"
abbr "sc-try-restart"="sudo systemctl try-restart"
abbr "sc-unmask"="sudo systemctl unmask"
abbr "sc-unset-environment"="sudo systemctl unset-environment"
abbr "scu-add-requires"="systemctl --user add-requires"
abbr "scu-add-wants"="systemctl --user add-wants"
abbr "scu-cancel"="systemctl --user cancel"
abbr "scu-cat"="systemctl --user cat"
abbr "scu-daemon-reexec"="systemctl --user daemon-reexec"
abbr "scu-daemon-reload"="systemctl --user daemon-reload"
abbr "scu-default"="systemctl --user default"
abbr "scu-disable"="systemctl --user disable"
abbr "scu-disable-now"="scu-disable --now"
abbr "scu-edit"="systemctl --user edit"
abbr "scu-emergency"="systemctl --user emergency"
abbr "scu-enable"="systemctl --user enable"
abbr "scu-enable-now"="scu-enable --now"
abbr "scu-failed"="systemctl --user --failed"
abbr "scu-get-default"="systemctl --user get-default"
abbr "scu-halt"="systemctl --user halt"
abbr "scu-help"="systemctl --user help"
abbr "scu-import-environment"="systemctl --user import-environment"
abbr "scu-is-active"="systemctl --user is-active"
abbr "scu-is-enabled"="systemctl --user is-enabled"
abbr "scu-is-failed"="systemctl --user is-failed"
abbr "scu-is-system-running"="systemctl --user is-system-running"
abbr "scu-isolate"="systemctl --user isolate"
abbr "scu-kexec"="systemctl --user kexec"
abbr "scu-kill"="systemctl --user kill"
abbr "scu-link"="systemctl --user link"
abbr "scu-list-dependencies"="systemctl --user list-dependencies"
abbr "scu-list-jobs"="systemctl --user list-jobs"
abbr "scu-list-machines"="systemctl --user list-machines"
abbr "scu-list-sockets"="systemctl --user list-sockets"
abbr "scu-list-timers"="systemctl --user list-timers"
abbr "scu-list-unit-files"="systemctl --user list-unit-files"
abbr "scu-list-units"="systemctl --user list-units"
abbr "scu-load"="systemctl --user load"
abbr "scu-mask"="systemctl --user mask"
abbr "scu-mask-now"="scu-mask --now"
abbr "scu-preset"="systemctl --user preset"
abbr "scu-preset-all"="systemctl --user preset-all"
abbr "scu-reenable"="systemctl --user reenable"
abbr "scu-reload"="systemctl --user reload"
abbr "scu-reload-or-restart"="systemctl --user reload-or-restart"
abbr "scu-rescue"="systemctl --user rescue"
abbr "scu-reset-failed"="systemctl --user reset-failed"
abbr "scu-restart"="systemctl --user restart"
abbr "scu-revert"="systemctl --user revert"
abbr "scu-set-default"="systemctl --user set-default"
abbr "scu-set-environment"="systemctl --user set-environment"
abbr "scu-set-property"="systemctl --user set-property"
abbr "scu-show"="systemctl --user show"
abbr "scu-show-environment"="systemctl --user show-environment"
abbr "scu-start"="systemctl --user start"
abbr "scu-status"="systemctl --user status"
abbr "scu-stop"="systemctl --user stop"
abbr "scu-switch-root"="systemctl --user switch-root"
abbr "scu-try-reload-or-restart"="systemctl --user try-reload-or-restart"
abbr "scu-try-restart"="systemctl --user try-restart"
abbr "scu-unmask"="systemctl --user unmask"
abbr "scu-unset-environment"="systemctl --user unset-environment"
abbr "tf"="terraform"
abbr "which-command"="whence"
